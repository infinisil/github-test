name: Cancellation test

on: pull_request

# Needed to create PRs
permissions:
  contents: write
  pull-requests: write

jobs:
  cancel-test:
    name: Test cancellation
    runs-on: ubuntu-latest
    steps:
      - name: Update pin
        id: update
        run: |
          echo hi
